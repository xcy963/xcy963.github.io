# 实验4和实验5
```{note}
关于vscode配置intellisense:
- 核心是生成`compile_commands.json`,使用bear,`bear -- make all`
- 之后等他生成就好了,注意如果之前编译过的话需要使用make clean,不然只能生成空文件
```

## 向磁盘里面添加数据
- 就是向我们的img文件添加数据,使用ubuntu的磁盘挂载器挂载然后往里面放就好了
- 这个是老的linux的文件系统,现在的ubuntu挂载需要创建一个loop设备
```bash
hitcrt@hitcrt-OMEN:~/rust_learning/bochs$ sudo losetup -Pf --show ./hdc-0.11.img
/dev/loop19
hitcrt@hitcrt-OMEN:~/rust_learning/bochs$ lsblk /dev/loop19
NAME       MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS
loop19       7:19   0 60.6M  0 loop 
└─loop19p1 259:18   0 60.6M  0 part 
hitcrt@hitcrt-OMEN:~/rust_learning/bochs$ sudo modprobe minix
hitcrt@hitcrt-OMEN:~/rust_learning/bochs$ sudo mount -t minix -o rw /dev/loop19p1 ./temp
hitcrt@hitcrt-OMEN:~/rust_learning/bochs$ 
```

- 卸载使用这个
```bash
sudo umount ./temp
sudo losetup -d /dev/loop19
```
## 关于中断调用

- idt在这个地方被抽象成一个数组,每个数组的元素是函数的入口地址`linux-0.11/include/linux/sys.h`定义了这个数组
- 发现其实linux的中断也是类似rcore的,他是先使用int 80,然后根据传入的参数选择对应的函数
  - 也就是说linux011使用中断由用户态转到内核态的汇编入口函数入口是`linux-0.11/kernel/system_call.s`里面的`system_call`
  - 然后使用eax选择对应的函数,似乎不是把操作系统的中断处理函数放到系统的idt里面的,但是linux0.00是放到IDT里面的

### 用户态转内核态的入口函数`system_call`

```text
system_call:
    cmpl $nr_system_calls-1,%eax #检查系统调用编号是否在合法范围内
    ja bad_sys_call
    push %ds
    push %es
    push %fs
    pushl %edx
    pushl %ecx
    pushl %ebx        # push %ebx,%ecx,%edx，是传递给系统调用的参数
    movl $0x10,%edx        # 让ds,es指向GDT，内核地址空间
    mov %dx,%ds
    mov %dx,%es
    movl $0x17,%edx        # 让fs指向LDT，用户地址空间
    mov %dx,%fs
    call sys_call_table(,%eax,4)
    pushl %eax
    movl current,%eax
    cmpl $0,state(%eax)
    jne reschedule
    cmpl $0,counter(%eax)
    je reschedule
```

```{note}
只需要知道他做了这些事情就好
- 1. 根据eax选择对应的函数
- 2. 函数调用结束之后eax是他的返回值
- 3. 函数调用的参数放在`EBX`,`ECX`,`EDX`
  - ```text
    [esp+0]   return address
    [esp+4]   第1个参数 = EBX
    [esp+8]   第2个参数 = ECX
    [esp+12]  第3个参数 = EDX
    ```
  - 由于系统调用只是支持三个函数,所有相关的函数调用也只有三个
```

## 修改的记录
- 1. 先注册我们的函数在`linux-0.11/include/linux/sys.h`
  - 使用`extern`符号引入相关函数,然后在`sys_call_table`的最后加入我们的函数
- 2. 修改makefile,使得我们的函数被编译
  - `OBJS  = sched.o system_call.o traps.o asm.o fork.o \
	panic.o printk.o vsprintf.o sys.o exit.o \
	signal.o mktime.o`
- 3. 用户程序也需要修改,因为磁盘上面的`unistd.h`里面没有我们的系统调用编号,所以我写在用户程序里面


## 实验5:

```{note}
需要修改的有以下文件:
- init/main.c 、 kernel/fork.c 和 kernel/sched.c
```


### 关于用户态的函数`times(&start_time);`
```{note}
底层是系统调用,算了管他是什么系统调用他就是填一个结构体

```c
struct tms
  {
    clock_t tms_utime;		/* User CPU time.  */
    clock_t tms_stime;		/* System CPU time.  */

    clock_t tms_cutime;		/* User CPU time of dead children.  */
    clock_t tms_cstime;		/* System CPU time of dead children.  */
  };
```

### 修改的记录

- 最后是和之前一样初始化文件系统,不过这一次是在pid为1的进程初始化的时候把文件也给一份pid为0的
X 可以是 N , J , R , W 和 E 中的任意一个，分别表示进程新建( N )、进入就绪态( J )、进入运行态( R )、进入阻塞态( W )和退出( E )；

linux-0.11/kernel/fork.c:136 log_process_state(p, 'N')：新进程刚被克隆完成，记录一次“创建”事件（N）。
linux-0.11/kernel/fork.c:138 log_process_state(p, 'J')：把新进程状态设为 TASK_RUNNING（可调度），记为“就绪/可运行”事件（J），表示它开始参与调度队列。
- 这两个就是fork的

linux-0.11/kernel/sched.c:133 log_process_state(*p, 'J')：在调度循环里发现可中断睡眠的任务收到信号，被置回 TASK_RUNNING，记录它重新进入就绪队列。
linux-0.11/kernel/sched.c:154 log_process_state(current, 'J')：当前正在运行的任务被换下 CPU（时间片用尽或被抢占），记一次“回到就绪队列”。
linux-0.11/kernel/sched.c:155 log_process_state(task[next], 'R')：即将被调度上的任务切换到运行态，记录“开始运行”。
linux-0.11/kernel/sched.c:163 log_process_state(current, 'W')：sys_pause 把自己设为 TASK_INTERRUPTIBLE 后，记录“等待/睡眠”。
linux-0.11/kernel/sched.c:180 log_process_state(current, 'W')：sleep_on 将当前任务改为 TASK_UNINTERRUPTIBLE，记录进入不可中断睡眠。
linux-0.11/kernel/sched.c:184 log_process_state(tmp, 'J')：sleep_on 返回时，如果之前队列里还有旧任务，把它标记为可运行（state=0）并记录重新就绪。
linux-0.11/kernel/sched.c:199 log_process_state(current, 'W')：interruptible_sleep_on 进入可中断睡眠时记录等待。
linux-0.11/kernel/sched.c:203 log_process_state(*p, 'J')：interruptible_sleep_on 被其他任务占了等待槽，唤醒该任务为可运行并记录。
linux-0.11/kernel/sched.c:209 log_process_state(tmp, 'J')：interruptible_sleep_on 最终返回前，唤醒原先的等待者并记录它就绪。
linux-0.11/kernel/sched.c:217 log_process_state(*p, 'J')：wake_up 把等待队列里的任务置为可运行并记录它重新就绪。
linux-0.11/kernel/exit.c:106 log_process_state(current, 'E')：进程开始退出流程，记录一次“退出”事件。
linux-0.11/kernel/exit.c:188 log_process_state(current, 'W')：waitpid 找不到满足条件的子进程且需要阻塞时，把当前进程置为可中断睡眠，记录“等待”。

- 对所有的进程id的理解:
- 0号是之后一直停机的进程
- 1号是负责回收终端的,因为他在等待终端的pid
- 2号是终端
- 