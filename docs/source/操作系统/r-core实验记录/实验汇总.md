# 总结所有的实验

## 关于数据结构的总结

### b树

- 在操作系统里面,我想要构建inode_id(一个u32的id号,和文件的物理位置相对应)-> 这个文件建立的连接数量
  - 这个存在的目的是删除文件数据块当且仅当他的链接数归零

### 二叉堆
- 调度的时候需要弹出stride最小的进程,所以需要进程被维护成一个堆

```rust
use std::collections::{BTreeMap, BinaryHeap};
use std::cmp::Reverse;

fn demo_btreemap() {
    println!("== BTreeMap demo: inode -> nlink ==");

    let mut link_counts: BTreeMap<u32, u32> = BTreeMap::new();

    // 初始化：不存在则插入 1
    link_counts.entry(10).or_insert(1);
    link_counts.entry(3).or_insert(1);

    // inc：有则 +1；无则先插入 0 再 +1（更符合“第一次变成 1”）
    *link_counts.entry(10).or_insert(0) += 1; // 10: 2
    *link_counts.entry(10).or_insert(0) += 1; // 10: 3
    *link_counts.entry(8).or_insert(0) += 1;  // 8: 1

    // get
    println!("nlink(10) = {:?}", link_counts.get(&10));
    println!("nlink(8)  = {:?}", link_counts.get(&8));

    // 有序遍历（key 从小到大）
    println!("All entries in order:");
    for (inode, nlink) in link_counts.iter() {
        println!("  inode={inode}, nlink={nlink}");
    }

    // 范围遍历（BTreeMap 很适合这个）
    println!("Range [5..=10]:");
    for (inode, nlink) in link_counts.range(5..=10) {
        println!("  inode={inode}, nlink={nlink}");
    }

    // 删除
    link_counts.remove(&3);
    println!("After remove inode=3: {:?}", link_counts);
}

fn demo_binaryheap() {
    println!("\n== BinaryHeap demo: scheduler ==");

    // 1) 默认最大堆：pop() 弹出最大
    let mut max_heap: BinaryHeap<i32> = BinaryHeap::new();
    max_heap.push(2);
    max_heap.push(10);
    max_heap.push(5);
    println!("max_heap.peek() = {:?}", max_heap.peek()); // Some(10)
    while let Some(x) = max_heap.pop() {
        println!("max_heap.pop() -> {x}");
    }

    // 2) 最小堆：用 Reverse 包起来
    // 模拟“定时任务”：按 time 最早的先执行
    let mut min_heap: BinaryHeap<Reverse<(u64, &'static str)>> = BinaryHeap::new();
    min_heap.push(Reverse((30, "flush log")));
    min_heap.push(Reverse((10, "wake task A")));
    min_heap.push(Reverse((20, "wake task B")));

    println!("\nmin-heap scheduler order:");
    while let Some(Reverse((time, name))) = min_heap.pop() {
        println!("time={time:>2} -> {name}");
    }
}

fn main() {
    demo_btreemap();
    demo_binaryheap();
}


```

## 关于rust的新鲜事情

### 1. 从结构体到字节数组

- 从一个结构体中,读它的内存数据,后续的用途是把他写到磁盘上面

```rust


```


## 关于实验使用的qemu模拟器还有他和vscode的联动debug

- 当操作系统开始跑我的终端应用程序之后,他就不会自动推出了
  - 这里可以使用qemu模拟器的强制退出机制按下`ctrl+a`然后输入一个`x`就可以强制退出
- 然后是关于gdb,gdb支持使用服务器的方式连接,在makefile里面写好了一个qemu模拟器开始debug的方式
```makefile
gdbserver: build
	@qemu-system-riscv64 -M 128m -machine virt -nographic -bios $(BOOTLOADER) -device loader,file=$(KERNEL_BIN),addr=$(KERNEL_ENTRY_PA) \
	-drive file=$(FS_IMG),if=none,format=raw,id=x0 \
        -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 \
	-s -S
```

- 我们打开这个服务器,然后在gdb里面测试连接(先保证gdb可以使用,我们再使用vscode的插件进行加载)

```bash
root@hitcrt-OMEN:/mnt/debug_tools/bin$ ./riscv64-unknown-elf-gdb /mnt/os/target/riscv64gc-unknown-none-elf/deb
ug/os
GNU gdb (SiFive GDB 8.3.0-2020.04.1) 8.3
Copyright (C) 2019 Free Software Foundation, Inc.
License GPLv3+: GNU GPL version 3 or later <http://gnu.org/licenses/gpl.html>
This is free software: you are free to change and redistribute it.
There is NO WARRANTY, to the extent permitted by law.
Type "show copying" and "show warranty" for details.
This GDB was configured as "--host=x86_64-linux-gnu --target=riscv64-unknown-elf".
Type "show configuration" for configuration details.
For bug reporting instructions, please see:
<https://github.com/sifive/freedom-tools/issues>.
Find the GDB manual and other documentation resources online at:
    <http://www.gnu.org/software/gdb/documentation/>.

For help, type "help".
Type "apropos word" to search for commands related to "word"...
Reading symbols from /mnt/os/target/riscv64gc-unknown-none-elf/debug/os...
(gdb) set architecture riscv:rv64
The target architecture is assumed to be riscv:rv64
(gdb) target remote localhost:1234
Remote debugging using localhost:1234
0x0000000000001000 in ?? ()
(gdb) info registers
ra             0x0      0x0
sp             0x0      0x0
```
- 加载gdb成功之后需要在vscode中使用,不然这个命令行有点吓人
  - 1. 需要在vscode的启动配置文件中加入源码的文件目录,不然打断点不好生效(最后发现其实vscode会自己去找的...)
  - 2. 需要开启断在开头,不然gdb一连上就开始跑根本不知道停在哪里
  - 3. 这个和bochs不一样没有ui,但是可以打文件的断点

- 这个是vscode的配置
```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RISC-V Kernel (QEMU gdb stub)",
      "type": "cppdbg",
      "request": "launch",

      "program": "${workspaceFolder}/os/target/riscv64gc-unknown-none-elf/release/os",
      "cwd": "${workspaceFolder}/os",

      "MIMode": "gdb",
      "miDebuggerPath": "/mnt/debug_tools/bin/riscv64-unknown-elf-gdb",

      "miDebuggerServerAddress": "127.0.0.1:1234",

      "launchCompleteCommand": "None",
      "stopAtEntry": false,

      "setupCommands": [
        { "text": "set architecture riscv:rv64" },
        { "text": "set pagination off" },
        { "text": "set confirm off" },
        { "text": "add-symbol-file /mnt/user/target/riscv64gc-unknown-none-elf/release/ch8b_user_shell 0x80400000" }
      ],

      "logging": {
        "engineLogging": true,
        "trace": true,
        "traceResponse": true
      }
    }
  ]
}
```

### 怎么在用户程序里面停住?

- 由于我们打开了虚拟地址,所以用户程序都是从一个基础地址开始的程序,目前还没有研究出来怎么停住,
- 说起来汇编的文件也停不住,估计是文件没有放好


