对，这里有两个“名字体系”搅在一起了，第一次看很容易迷糊，你这个疑问**非常关键**👍

---

## 1. 为啥既叫 `x2` 又叫 `sp`，不会冲突吗？

**不会冲突，因为它们是同一个寄存器的两个“别名”。**

* 在 **RISC-V 架构里**，整数寄存器就叫：
  `x0, x1, x2, ..., x31` —— 这是**硬件视角**（architectural name）
* 在 **ABI / 调用约定里**，为了方便人类记，把常用寄存器起了“功能名”：

  * `x0` → `zero`（永远是 0）
  * `x1` → `ra`（return address）
  * `x2` → `sp`（stack pointer）
  * `x3` → `gp`（global pointer）
  * `x4` → `tp`（thread pointer）
  * `x5~x7` → `t0~t2`（temporaries）
  * `x8~x9` → `s0/fp, s1`（saved regs / frame pointer）
  * `x10~x17` → `a0~a7`（arguments / return values）
  * `x18~x27` → `s2~s11`（saved regs）
  * `x28~x31` → `t3~t6`（temporaries）

**在汇编器里：**

* 写 `x2` 和写 `sp`，生成的机器码是完全一样的
* 只是 `sp` 这个名字对人更友好：你一看就知道“这是栈指针”，而不是“寄存器编号 2”

所以：

> ✅ `x2` 和 `sp` 是**同一个物理寄存器**
> ✅ 只是写汇编时可以选你喜欢的名字
> ❌ 不是两个不同的寄存器

你在 trap 的汇编里：

```asm
csrrw sp, sscratch, sp
```

这里用的是 `sp` 这个“功能名”；
如果你写成：

```asm
csrrw x2, sscratch, x2
```

也一样完全正确，只是可读性差一点。

---

## 2. RISC-V 真的只有 32 个寄存器吗？

要分几类说：

### 2.1 整数通用寄存器

是的，**整数通用寄存器（GPR，General Purpose Registers）就 32 个**：

```text
x0 ~ x31   （每个是 32/64 位，取决于是 RV32 还是 RV64）
```

你现在玩内核实验、写汇编、切栈，这些主要用的就是这 32 个 GPR。

---

### 2.2 还有别的寄存器吗？

有，但不是“整数通用寄存器”，是**其他寄存器文件或特殊寄存器**：

* **浮点寄存器**：`f0 ~ f31`（如果实现了 F/D 扩展）
* **向量寄存器**：`v0 ~ v31`（如果实现了 V 扩展）
* **CSR（控制状态寄存器）**：
  像你已经在用的：

  * `sstatus`
  * `sepc`
  * `stvec`
  * `sscratch`
  * `scause`
  * `satp`
  * ……这些都是**特殊的控制寄存器**，通过 `csrrw/csrrs/csrr` 访问

所以从“**内核写汇编**”的角度看：

* 通用整数寄存器：32 个（x0~x31）
* 外加很多**CSR**，但它们不算 GPR

---

## 3. 你可以怎么理解这两套名字？

直接给你一个心里模型：

* **硬件层级：**

  * RISC-V 规定：每个 hart 有 32 个整数寄存器，名字是 `x0..x31`
* **ABI / 平台约定：**

  * 系统 / 编译器 / OS 之间约定：

    * 调用函数时，用 `a0/a1` 传参
    * 用 `sp` 当栈指针
    * 用 `ra` 存返回地址……
  * 这些都是用 **功能名（别名）** 来表达这 32 个寄存器的用途

汇编器帮你做了一层映射：

| 硬件名     | ABI 名  | 通常用途                     |
| ------- | ------ | ------------------------ |
| x0      | zero   | 恒为 0                     |
| x1      | ra     | 返回地址                     |
| x2      | sp     | 栈指针                      |
| x3      | gp     | 全局指针                     |
| x4      | tp     | 线程指针                     |
| x5~x7   | t0~t2  | 临时寄存器（不需保存）              |
| x8~x9   | s0~s1  | callee-saved / frame ptr |
| x10~x17 | a0~a7  | 参数 & 返回值                 |
| x18~x27 | s2~s11 | callee-saved             |
| x28~x31 | t3~t6  | 临时寄存器                    |

你在写 trap 保存上下文的时候，之所以把很多寄存器都存起来，是为了在从内核返回到用户态时，**能把用户态的执行现场 1:1 恢复回去**。

---

## 4. 总结一句话

* **`x2` 和 `sp` 不会冲突，因为它们是“同一个寄存器”的两个不同名字**：

  * `x2` 是“编号”
  * `sp` 是“角色：栈指针”
* RISC-V 里：

  * **整数通用寄存器就 32 个：x0 ~ x31**
  * 另外还有浮点寄存器、向量寄存器、CSR 等别的寄存器，但它们是不同类别

如果你想，我可以帮你画一张表，把**TrapContext.x[0..31]** 和 **ABI 名（sp/ra/a0...）** 挨个对上，方便你在调试的时候看寄存器更直观。
